const LambdaTester = require("lambda-tester");
const serviceQrcodeDetailsHandler = require("../src/index").performQrcodeDetails;

describe("QR code", function () {
  it("Is service running", () => {
    // to be implemented
    expect(true).toEqual(true);
  });
});
